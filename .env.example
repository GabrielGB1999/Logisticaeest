JWT_SECRET=cambiar_por_clave_secreta_segura
PORT=5000

# Contraseña del usuario "admin" que se crea automáticamente la primera vez
# que arranca el sistema (solo en una base de datos nueva).
# Si no se define, se usa una por defecto que es PÚBLICA: definila acá antes
# del primer arranque, o cambiala luego desde Administración → Usuarios.
# ADMIN_PASSWORD=

# Email (opcional - para alertas por correo)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=tucuenta@gmail.com
# SMTP_PASS=tu_app_password
# ALERT_EMAIL_TO=admin@escuela.edu.ar
